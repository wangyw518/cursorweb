# taiqiu/audio

BGM is an original 8-bar pentatonic loop synthesized at runtime in `js/sfx.js`
(`BGM_NOTES` + WebAudio / `wx.createWebAudioContext`). No copyrighted recording
is bundled. Toggle from the HUD **音乐** button (default on).
